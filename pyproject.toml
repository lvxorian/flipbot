[project]
name = "flipbot"
version = "1.0.0"
description = "Realitní monitoring a analýza"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.115.0",
    "jinja2>=3.1.4",
    "python-dotenv>=1.1.0",
]

[tool.uv]
dev-dependencies = []
