[project]
name = "flipbot"
version = "1.0.0"
description = "Realitní monitoring a analýza"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.110",
    "jinja2>=3.1",
    "python-dotenv>=1.0",
]
